import React from 'react';

// DeviceTypeManagement removed — placeholder component
// Kept only to avoid import errors in other modules. The page has been removed per request.
const DeviceTypeManagement = () => null;

export default DeviceTypeManagement;
